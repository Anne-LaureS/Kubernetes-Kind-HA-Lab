apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-v1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo-v1
  template:
    metadata:
      labels:
        app: demo-v1
    spec:
      automountServiceAccountToken: false
      containers:
      - name: demo
        image: demo:v1
        ports:
        - containerPort: 80
        resources:
          requests:
            cpu: 100m
            memory: 64Mi
            ephemeral-storage: 128Mi
          limits:
            cpu: 200m
            memory: 128Mi
            ephemeral-storage: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: demo-v1
spec:
  selector:
    app: demo-v1
  ports:
  - port: 80
    targetPort: 80
